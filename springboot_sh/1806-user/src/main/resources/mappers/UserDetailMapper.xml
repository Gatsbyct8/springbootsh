<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sh.manage.mapper.UserDeatilMapper">
	<update id="updateUserDetail" parameterType="UserDetail">
	update user_detail set email = #{email} , birthday = #{birthday},sex=#{sex}
	where user_detail_id=#{userDetailId}
	</update>
</mapper>